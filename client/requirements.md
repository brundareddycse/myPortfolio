## Packages
framer-motion | Complex page transitions and scroll animations
react-icons | Additional icons if Lucide is insufficient (though Lucide is preferred)
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility to merge Tailwind classes without conflicts

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ['"Inter"', "sans-serif"],
  display: ['"Space Grotesk"', "sans-serif"],
}
